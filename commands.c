//
// Created by David Arroyo on 15/9/24.
//

#include "commands.h"

/**
 * función que habilita el listado de ficheros en el repos. actual
 * @param client_socket número del cliente conectado por socket
 */
void handle_list(int client_socket) {

}

/**
 * Función para recuperar un archivo desde el servidor, descargarlo
 * @param client_socket número del cliente conectado por socket
 * @param filename letra del archivo a descargar
 */
void handle_retr(int client_socket, const char *filename) {
 
}

/**
 * Función para elojar un fichero en el servidor
 * @param client_socket número del cliente conectado por socket
 * @param filename letra del archivo a descargar
 */
void handle_stor(int client_socket, const char *filename) {
 
}
